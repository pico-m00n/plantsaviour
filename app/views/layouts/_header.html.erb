<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-warning">
      <a class="navbar-brand text-dark" href="/"><span>Plantsaviour</span></a>

      <% if end_user_signed_in? %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                          aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav ml-auto">
              <div class="container">
                  <li class="px-2"><%= link_to '一覧', questions_path, method: :get, class: 'text-dark' %></li>
                  <li class="px-2"><%= link_to '投稿する', new_question_path, class: 'text-dark' %></li>
                  <li class="px-2"><%= link_to "ログアウト", destroy_end_user_session_path, method: :delete, class: 'text-dark' %></li>
              </div>
          </ul>
        </div>
      <% end %>

  </nav>
</header>